# 5.4 Normalização do Modelo de Dados

## Processo de Normalização

O esquema da base de dados do SGCLC foi projectado directamente em **Terceira Forma Normal (3NF)**, evitando anomalias de inserção, actualização e eliminação que são comuns em esquemas não normalizados. O processo de normalização foi conduzido de forma rigorosa, partindo de uma análise de dependências funcionais entre atributos.

## Verificação da 1NF (Primeira Forma Normal)

A **1NF** exige que todos os atributos sejam atómicos (sem grupos repetitivos ou atributos multi-valor).

| Critério | Estado no SGCLC |
|---|---|
| Ausência de grupos repetitivos | ✅ As linhas de venda residem em `LinhasVenda`, não em `Vendas` |
| Todos os atributos são atómicos | ✅ Não existem atributos compostos (ex. morada está numa única coluna) |
| Existência de chave primária em cada tabela | ✅ Todas as tabelas têm PK definida |

**Exemplo de aplicação:** Uma venda com múltiplos produtos não foi representada com arrays ou colunas repetidas em `Vendas`. Em vez disso, criou-se a tabela `LinhasVenda` com uma linha por produto vendido — cada linha referencia a `Venda` e o `Produto`.

## Verificação da 2NF (Segunda Forma Normal)

A **2NF** exige que todos os atributos não-chave dependam da **totalidade** da chave primária (sem dependências parciais).

Nas tabelas com chaves compostas (como `Stocks` com `(LojaId, ProdutoId)`):

| Atributo | Depende de LojaId? | Depende de ProdutoId? | Depende de ambos? | Conformidade |
|---|---|---|---|---|
| `Quantidade` | Não (varia por produto) | Não (varia por loja) | ✅ Sim | ✅ 2NF |
| `StockMinimo` | Não | Não | ✅ Sim | ✅ 2NF |
| `PrecoVendaLocal` | Não | Não | ✅ Sim | ✅ 2NF |

Nota: `NomeProduto` **não** está na tabela `Stocks` — estaria em dependência parcial (dependeria apenas de `ProdutoId`). Em vez disso, é lido via JOIN com `Produtos`, preservando a 2NF.

## Verificação da 3NF (Terceira Forma Normal)

A **3NF** exige que não existam dependências transitivas (atributos não-chave que dependam de outros atributos não-chave).

**Exemplo de potencial violação 3NF evitada:**  
Na tabela `Vendas`, poder-se-ia ter incluído `NomeLoja` e `EmailLoja`. Estes atributos dependeriam de `LojaId` (que depende da PK) — dependência transitiva. Solução: estes dados residem exclusivamente em `Lojas` e são obtidos via JOIN, mantendo a 3NF.

| Decisão de Design | Justificação (3NF) |
|---|---|
| `Vendas` não guarda nome/morada da loja | Esses dados dependem de `LojaId` → violação transitiva evitada |
| `LinhasVenda` guarda `PrecoUnitario` (desnormalização intencional) | O preço no momento da venda pode diferir do preço actual do produto — captura histórica necessária |
| `LinhasFatura` guarda `DescricaoProduto` (desnormalização intencional) | Uma fatura é um documento legal que não pode ser alterado se o produto mudar de nome |

## Desnormalizações Intencionais e Justificadas

Existem duas desnormalizações deliberadas no esquema, ambas justificadas por requisitos de rastreabilidade histórica:

1. **`LinhasVenda.PrecoUnitario`**: o preço praticado no momento da venda é registado na linha, mesmo que o `Produto.PrecoBaseVenda` seja alterado no futuro. Sem esta desnormalização, o recálculo de vendas passadas produziria valores incorrectos.

2. **`LinhasFatura.DescricaoProduto`**: uma fatura é um documento fiscal. Se o nome de um produto mudar, a fatura deve continuar a mostrar o nome que tinha no momento da emissão.

Estas desnormalizações são uma prática padrão em sistemas de comércio electrónico e são a excepção justificada à regra da 3NF, não uma violação inadvertida.
