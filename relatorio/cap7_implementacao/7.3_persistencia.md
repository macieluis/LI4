# 7.3 Camada de Persistência

## Padrão Repository

A camada de persistência do SGCLC implementa o **Repository Pattern**, que abstrai o acesso à base de dados e expõe ao resto da aplicação apenas as operações necessárias para cada entidade. Cada repositório implementa uma interface definida em `ConvenienceChain.Core/Interfaces/`.

Esta separação garante:
- **Testabilidade**: os serviços podem ser testados com implementações mock dos repositórios
- **Substituibilidade**: a base de dados pode ser trocada (ex. de SQLite para SQL Server) sem alterar nenhum serviço

## Repositórios Implementados

| Repositório | Interface | Métodos Principais |
|---|---|---|
| `ProdutoRepository` | `IProdutoRepository` | `GetAllAsync`, `GetByIdAsync`, `SearchAsync`, `CreateAsync`, `UpdateAsync` |
| `StockRepository` | `IStockRepository` | `GetByLojaAsync`, `GetByLojaAndProdutoAsync`, `UpdateQuantidadeAsync`, `GetAlertasAsync` |
| `VendaRepository` | `IVendaRepository` | `CreateAsync`, `GetByLojaAsync`, `GetByIdAsync`, `CancelAsync` |
| `EncomendaRepository` | `IEncomendaRepository` | `GetAllAsync`, `GetByLojaAsync`, `CreateAsync`, `UpdateEstadoAsync` |
| `FaturaRepository` | `IFaturaRepository` | `GetByLojaAsync`, `CreateAsync`, `GerarNumeroAsync` |
| `UtilizadorRepository` | `IUtilizadorRepository` | `GetByEmailAsync`, `GetAllAsync`, `CreateAsync`, `UpdateAsync` |
| `ConsolidacaoRepository` | `IConsolidacaoRepository` | `CreateAsync`, `GetByLojaAndDataAsync`, `GetHistoricoAsync` |
| `AjusteStockRepository` | `IAjusteStockRepository` | `CreateAsync`, `GetByLojaAsync` |

## Implementação da Pesquisa Insensível a Acentos

Um dos casos de implementação mais relevantes é a pesquisa de produtos insensível a acentos (RF09). A solução adoptada utiliza `NormalizationForm.FormD` para decompor os caracteres acentuados e remover as marcas de acento:

```csharp
// Em Repositories.cs — método SearchAsync
private static string Normalizar(string input)
{
    if (string.IsNullOrEmpty(input)) return input;
    var normalized = input.Normalize(NormalizationForm.FormD);
    var sb = new System.Text.StringBuilder();
    foreach (var c in normalized)
    {
        if (System.Globalization.CharUnicodeInfo.GetUnicodeCategory(c)
            != System.Globalization.UnicodeCategory.NonSpacingMark)
            sb.Append(c);
    }
    return sb.ToString().ToLowerInvariant();
}

public async Task<List<Produto>> SearchAsync(string termo)
{
    var termoNorm = Normalizar(termo);
    var todos = await _context.Produtos
        .Where(p => p.Ativo)
        .Include(p => p.Categoria)
        .ToListAsync();

    return todos.Where(p =>
        Normalizar(p.Nome).Contains(termoNorm) ||
        Normalizar(p.Codigo).Contains(termoNorm) ||
        Normalizar(p.Categoria?.Nome ?? "").Contains(termoNorm)
    ).ToList();
}
```

Esta mesma função `Normalizar` foi replicada no componente Blazor `POS/Index.razor` para o filtro client-side, garantindo comportamento consistente em toda a aplicação.

## Registo de Dependências

Todos os repositórios e serviços são registados no contentor de DI do ASP.NET Core em `Program.cs`:

```csharp
builder.Services.AddScoped<IProdutoRepository, ProdutoRepository>();
builder.Services.AddScoped<IStockRepository, StockRepository>();
builder.Services.AddScoped<IVendaRepository, VendaRepository>();
// ... restantes repositórios
builder.Services.AddScoped<IProdutoService, ProdutoService>();
builder.Services.AddScoped<IStockService, StockService>();
builder.Services.AddScoped<ISalesService, SalesService>();
// ... restantes serviços
builder.Services.AddSingleton<SessionService>();
builder.Services.AddHostedService<ConsolidacaoBackgroundService>();
```
